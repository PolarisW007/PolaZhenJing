# 需求记录：发布中心生产收口

日期：2026-06-02

## 需求来源

钉钉需求池 `XhcYwKVYha`：PolaZhenJing 发布中心生产收口：微信批量草稿、X 真实发帖与待提交清理。

## 目标

- 确认微信公众号批量草稿能力的生产状态。
- 确认 X 官方 API 发帖能力是否可进入真实发帖。
- 清理或明确服务器 `/PolaZhenjing` 中发布中心相关待提交状态。
- 保持生产服务和现有文章线不被误覆盖。

## 当前结论

- 本地 `main` 与 `origin/main` 已包含发布中心相关提交：
  - `fe67d6f fix: 稳定微信公众号批量草稿同步`
  - `56fec1a feat: 接入 X 自动发布`
  - `351df4b fix: 稳定上传页富文本编辑器加载`
- 本地工作树干净，发布中心测试通过。
- 线上 `polazj.service` 为 active，发布中心和上传页未登录访问均跳转登录页。
- 线上微信配置完整；线上 X token 未配置，因此不能完成真实 X 发帖。
- 服务器 `/PolaZhenjing` 工作树仍有发布中心相关 rsync 变更和文章/auth 变更，不能用粗暴 reset 清理。

## 非目标

- 不在文档、日志、数据库或 git diff 中写入第三方 token。
- 不自动修改生产 `.env`。
- 不覆盖服务器文章生成线、auth 改动或用户已有未提交文件。

## 验收标准

- A1 本地发布中心代码、测试和 git 状态明确。
- A2 线上服务、发布中心路由和配置状态明确。
- A3 X 真实发帖若缺少 token，必须明确阻塞原因，而不是误报完成。
- A4 服务器待提交清理必须保护文章/auth 现有改动，不做 destructive reset。
- A5 将本次核查、阻塞和下一步写入 devlog，并同步总 devlog。
